# "r"  <default> open file for read mode
#  "w" open a file for writing mode
# "x"  create a file if not exists
#  "a" Add content for a file
#  "t"  <default> open a file in a text (whenever we want to deal with String or text)
#  "B"  binary mode
#  "+"  read and write mode (we want update contents)